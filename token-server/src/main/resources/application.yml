spring:
  profiles.active: local
  application:
    name: sentinel-token-server
server:
  port: 8882

nacos:
  config:
    auto-refresh: true
    group: SENTINEL_GROUP
    bootstrap:
      enable: true
    type: json